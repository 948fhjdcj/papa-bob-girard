{
  "name": "papa-bob-gerard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy-pages": "wrangler pages deploy public --project-name papa-bob-site",
    "db:migrate": "wrangler d1 execute papa-bob-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
